/*'use strict';

describe("module: pictures", function() {
  var module;

  beforeEach(function() {
    module = module('app.pictures');
  });

  it("should be registered", function() {
    expect(module).not.to.equal(null);
  });
});*/
